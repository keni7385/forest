language: cpp
dist: xenial
os:
  - linux
script:
  - mkdir build
  - cd build
  - cmake .. -DBUILD_TESTS=ON -DENABLE_COVERAGE=ON
  - make -j2
  - ctest
  - cd test
  - bash <(curl -s https://codecov.io/bash)
